JENKINS_HOME="{{ jenkins_home }}"
BACKUP_DIR="{{ backup_dir }}"
RESTORE_DIR="{{ restore_dir }}"

SFTP_USER="{{ sftp_user }}"
SFTP_HOST="{{ sftp_host }}"
SFTP_PORT="{{ sftp_port }}"
REMOTE_DIR="{{ remote_dir }}"